#include <csolve.h>
#include <sys/types.h>
#include <sys/socket.h>

void
ui_init()
  OKEXTERN;

void
ui_kill(int sig)
  OKEXTERN;

pid_t
ui_proc()
  OKEXTERN;

int * START VALIDPTR ROOM_FOR(int) REF(? Set_emp([TAGSET([V])]))
ui_soc()
  OKEXTERN;
